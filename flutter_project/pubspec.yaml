name: flutter_project
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  proto_package:
    path: ../proto_package

dev_dependencies:
  generator_package:
    path: ../generator_package
  flutter_test:
    sdk: flutter
  flutter_lints:

flutter:
  uses-material-design: true
