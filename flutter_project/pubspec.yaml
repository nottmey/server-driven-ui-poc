name: flutter_project
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner:
  generator_library:
    path: ../generator_library
  flutter_test:
    sdk: flutter
  flutter_lints:

flutter:
  uses-material-design: true
